# Task C Proof Of Completion

## Scope Summary

- Task C owned files changed: `src/splitshot/browser/static/index.html`, `src/splitshot/browser/static/app.js`, `src/splitshot/browser/static/styles.css`, `tests/browser/test_browser_interactions.py`, `tests/browser/test_browser_static_ui.py`, `tests/browser/test_browser_control_inventory_audit.py`, `tests/browser/test_browser_control_coverage_matrix.py`, `docs/userfacing/panes/project.md`, `docs/project/browser-control-qa-matrix.md`, `.github/copilot-instructions.md`, `.github/AGENTS.md`, `.github/instructions/practiscore-sync.instructions.md`.
- Shared orchestration artifacts changed: `development/agentstatus.md`, `development/task-c-proof-of-completion.md`.
- Ownership note: the repository already contained Task A, Task B, and planning-file changes in the worktree before Task C execution. Task C edits stayed within Task C-owned files plus the allowed shared orchestration artifacts.

## Prerequisite Contracts

| Row | Checklist item | Status | Evidence | Validation note |
| --- | --- | --- | --- | --- |
| P1 | 1. Task A provides the session, match-list, and selected-match import route surface. | complete | `src/splitshot/browser/static/app.js` now calls `POST /api/practiscore/session/start`, `GET /api/practiscore/session/status`, `GET /api/practiscore/matches`, `POST /api/practiscore/sync/start`, and `POST /api/practiscore/session/clear`; `tests/browser/test_browser_interactions.py` exercises those routes through the Project pane. | Task C consumes the Task A route surface directly instead of reimplementing session or import endpoints. |
| P2 | 2. Task B provides stable `/api/state` payloads: `practiscore_session`, `practiscore_sync`, `practiscore_options`. | complete | `src/splitshot/browser/static/app.js` normalizes and renders `state.practiscore_session`, `state.practiscore_sync`, and `state.practiscore_options`; `.github/copilot-instructions.md` codifies those three payloads as the browser contract. | The Project pane implementation is built on the exact Task B payload surface. |
| P3 | 3. Task B keeps `practiscore_options` as the local-stage source for `Match type`, `Stage #`, `Competitor name`, and `Place` after sync completes. | complete | `src/splitshot/browser/static/app.js` refreshes full browser state after successful remote import and continues rendering the existing local controls; `tests/browser/test_browser_interactions.py::test_project_pane_practiscore_connect_match_list_and_selected_match_import_flow` waits for `practiscore_options.has_source` and the stage controls to populate. | Task C keeps the local staged PractiScore controls authoritative after sync instead of replacing them with a separate remote-only model. |

## Code Directions

| Row | Checklist item | Status | Evidence | Validation note |
| --- | --- | --- | --- | --- |
| C1 | 1. Update `src/splitshot/browser/static/index.html` Project pane PractiScore section to include `Connect PractiScore`, the available remote match select, `Import Selected Match`, `Clear PractiScore Session`, and a persistent session/sync status area. | complete | `src/splitshot/browser/static/index.html` now adds `connect-practiscore`, `practiscore-remote-match`, `import-practiscore-selected`, `clear-practiscore-session`, and the `practiscore-session-sync-status` status block; `tests/browser/test_browser_static_ui.py` asserts the new IDs and labels. | The Project pane markup now exposes the full Task C remote PractiScore control surface. |
| C2 | 2. Keep the existing local PractiScore controls and `Select PractiScore File` button in place as the manual fallback path. | complete | `src/splitshot/browser/static/index.html` retains `match-type`, `match-stage-number`, `match-competitor-name`, `match-competitor-place`, and `import-practiscore`; `docs/userfacing/panes/project.md` documents the fallback path; `tests/browser/test_browser_interactions.py::test_project_pane_manual_practiscore_file_import_remains_functional_with_remote_controls` passed. | Task C adds remote sync controls without removing the existing local file-import path or local match-context controls. |
| C3 | 3. Update `src/splitshot/browser/static/app.js`. | complete | `src/splitshot/browser/static/app.js` now includes PractiScore payload normalization, remote session polling, route-specific request helpers, status rendering, remote match option rendering, and event wiring; `tests/browser/test_browser_static_ui.py` asserts the new function surface. | The Task C behavior lives in the owned Project-pane browser shell file, as required. |
| C4 | 4. Add a handler for `Connect PractiScore` that calls `POST /api/practiscore/session/start`. | complete | `src/splitshot/browser/static/app.js` adds `connectPractiScore()` and the `connect-practiscore` click handler, which calls `requestPractiScorePayload("/api/practiscore/session/start", {})`; `tests/browser/test_browser_static_ui.py` asserts that route call and handler wiring. | The Connect button now drives the Task A session-start route from the browser UI. |
| C5 | 5. Poll `GET /api/practiscore/session/status` only during the active connect flow. | complete | `src/splitshot/browser/static/app.js` adds `practiScoreConnectFlowActive`, `schedulePractiScoreSessionPoll()`, `pollPractiScoreSessionStatus()`, and `stopPractiScoreSessionPolling()`; status polling is scheduled only while the connect flow is active. | The Project pane now polls session status narrowly during authentication instead of continuously. |
| C6 | 6. When the session becomes `authenticated_ready`, request available remote matches through `GET /api/practiscore/matches`. | complete | `src/splitshot/browser/static/app.js` calls `loadPractiScoreRemoteMatches()` from both `connectPractiScore()` and `pollPractiScoreSessionStatus()` when the session reaches `authenticated_ready`; `tests/browser/test_browser_interactions.py::test_project_pane_practiscore_connect_match_list_and_selected_match_import_flow` waits for `match_list_ready`. | The UI transitions directly from a ready session to remote match discovery, matching the task contract. |
| C7 | 7. Populate the remote match select with the stable match objects from Task B. | complete | `src/splitshot/browser/static/app.js` adds `normalizePractiScoreRemoteMatches()` and `renderPractiScoreRemoteMatchOptions()` to populate `practiscore-remote-match` from `practiscore_sync.matches`; the happy-path interaction test selects `match-200` successfully. | The remote match select is now driven by the stable Task B match object shape. |
| C8 | 8. Add a handler for `Import Selected Match` that calls `POST /api/practiscore/sync/start` with the selected `remote_id`. | complete | `src/splitshot/browser/static/app.js` adds `importSelectedPractiScoreMatch()` and the `import-practiscore-selected` click handler, which posts `{ remote_id: remoteId }` to `/api/practiscore/sync/start`; `tests/browser/test_browser_static_ui.py` asserts the route call and `tests/browser/test_browser_interactions.py` exercises the flow. | The Project pane can now import one selected remote match through the Task B sync route. |
| C9 | 9. Add a handler for `Clear PractiScore Session` that calls `POST /api/practiscore/session/clear`. | complete | `src/splitshot/browser/static/app.js` adds `clearPractiScoreSession()` and the `clear-practiscore-session` click handler, which posts to `/api/practiscore/session/clear`; `tests/browser/test_browser_static_ui.py` asserts the handler and route call. | The UI now exposes explicit session reset behavior through the Task A clear route. |
| C10 | 10. Render a stable UI state machine across the Project pane for `not_authenticated`, `authenticating`, `authenticated_ready`, `match_list_ready`, `importing_selected_match`, `success`, `expired`, and `error`. | complete | `src/splitshot/browser/static/app.js` adds `currentPractiScoreUiState()` and `renderPractiScoreRemoteState()` to map session/sync payloads into stable UI states and status-pill data attributes; `tests/browser/test_browser_interactions.py` covers both the success path and expired-session rendering. | The Project pane now renders the required end-user state machine instead of ad hoc status text. |
| C11 | 11. Disable the remote-match import action until a session is ready and a remote match is selected. | complete | `src/splitshot/browser/static/app.js::renderPractiScoreRemoteState()` disables `import-practiscore-selected` until the session is `authenticated_ready`, a remote match is selected, and no import is in progress; the expired-session interaction test verifies the disabled state. | Import actions are now gated behind the required ready-session and selected-match conditions. |
| C12 | 12. After a successful selected-match import, continue rendering the existing local PractiScore controls from `practiscore_options`. | complete | `src/splitshot/browser/static/app.js::importSelectedPractiScoreMatch()` refreshes full browser state on sync success, and `renderControls()` continues to synchronize `match-type`, `match-stage-number`, `match-competitor-name`, and `match-competitor-place`; the happy-path interaction test waits for populated local controls and imported stage state. | Remote sync now feeds the existing local PractiScore controls instead of bypassing them. |
| C13 | 13. Preserve existing manual file import flow and existing competitor/place synchronization behavior. | complete | `src/splitshot/browser/static/app.js` preserves the `practiscore-file-input` change handler and `syncPractiScoreSelectionFields()` behavior; `tests/browser/test_browser_interactions.py::test_project_pane_manual_practiscore_file_import_remains_functional_with_remote_controls` passed, and the PractiScore regression slices remained green. | Task C adds remote sync without breaking the preexisting manual import and competitor-place sync semantics. |
| C14 | 14. Update `src/splitshot/browser/static/styles.css` with the status and layout styles needed for the new controls without disturbing the current Project-pane structure. | complete | `src/splitshot/browser/static/styles.css` now defines `.practiscore-remote-panel`, `.practiscore-session-sync-status`, `.practiscore-status-pill`, and related state styles; `tests/browser/test_browser_static_ui.py` asserts the new selectors. | The new PractiScore UI is styled in the owned CSS surface without restructuring the wider Project pane. |

## Test Directions

| Row | Checklist item | Status | Evidence | Validation note |
| --- | --- | --- | --- | --- |
| T1 | 1. Update `tests/browser/test_browser_static_ui.py` for the new Project-pane control IDs and labels. | complete | `tests/browser/test_browser_static_ui.py` now asserts the new PractiScore IDs, labels, CSS selectors, JS route handlers, and button inventory. | The static browser-shell contract now explicitly owns the new Task C Project-pane controls. |
| T2 | 2. Update `tests/browser/test_browser_control_inventory_audit.py` for the new control IDs. | complete | `tests/browser/test_browser_control_inventory_audit.py` now includes `connect-practiscore`, `clear-practiscore-session`, `practiscore-remote-match`, and `import-practiscore-selected`, and updates the expected inventory count to `272`. | The exhaustive mutable-control inventory now accounts for the new PractiScore UI surface. |
| T3 | 3. Update `tests/browser/test_browser_interactions.py`. | complete | `tests/browser/test_browser_interactions.py` now adds fake PractiScore session helpers, remote-match artifact builders, and Task C Project-pane interaction tests. | The owned browser interaction suite now covers the new Project-pane sync flow. |
| T4 | 4. Add a UI flow test for connect, match-list load, remote-match selection, and selected-match import. | complete | `tests/browser/test_browser_interactions.py::test_project_pane_practiscore_connect_match_list_and_selected_match_import_flow` passed. | The happy-path Task C remote sync flow is directly covered by a named browser interaction test. |
| T5 | 5. Add a UI flow test for expired-session or error-state rendering. | complete | `tests/browser/test_browser_interactions.py::test_project_pane_practiscore_expired_session_state_disables_selected_match_import` passed. | Expired-session state rendering and action disabling are directly covered by a named browser interaction test. |
| T6 | 6. Add a UI flow test that manual `Select PractiScore File` import remains functional after the new controls are added. | complete | `tests/browser/test_browser_interactions.py::test_project_pane_manual_practiscore_file_import_remains_functional_with_remote_controls` passed. | Manual fallback parity is directly covered after adding the remote sync controls. |
| T7 | 7. Update `tests/browser/test_browser_control_coverage_matrix.py` to include the new Project/import controls and explicit test ownership. | complete | `tests/browser/test_browser_control_coverage_matrix.py` was updated alongside `docs/project/browser-control-qa-matrix.md`; `runTests` on that file passed with `2 passed, 0 failed`. | The QA-matrix enforcement test now matches the new PractiScore control and coverage claims. |
| T8 | 8. Run `uv run pytest tests/browser/test_browser_interactions.py tests/browser/test_browser_static_ui.py tests/browser/test_browser_control_inventory_audit.py tests/browser/test_browser_control_coverage_matrix.py` | complete | Command run: `uv run pytest tests/browser/test_browser_interactions.py tests/browser/test_browser_static_ui.py tests/browser/test_browser_control_inventory_audit.py tests/browser/test_browser_control_coverage_matrix.py` -> `55 passed in 93.97s (0:01:33)`. | The exact required Task C browser UI test slice passed under the specified command. |
| T9 | 9. Run PractiScore browser regressions: `uv run pytest tests/browser/test_browser_control.py -k practiscore`; `uv run pytest tests/browser/test_project_lifecycle_contracts.py -k practiscore` | complete | Command run: `uv run pytest tests/browser/test_browser_control.py -k practiscore` -> `7 passed, 63 deselected in 4.19s`; command run: `uv run pytest tests/browser/test_project_lifecycle_contracts.py -k practiscore` -> `2 passed, 5 deselected in 1.33s`. | Existing PractiScore browser regressions remained green after the Task C browser-shell changes. |

## Documentation And Governance Directions

| Row | Checklist item | Status | Evidence | Validation note |
| --- | --- | --- | --- | --- |
| D1 | 1. Update `docs/userfacing/panes/project.md` control table and how-to steps to include Connect, remote-match selection, selected-match import, clear-session behavior, and manual fallback. | complete | `docs/userfacing/panes/project.md` now documents the remote sync controls, status area, clear-session action, and manual fallback flow in both the control table and numbered usage steps. | The user-facing Project-pane guide now matches the Task C workflow exposed by the browser UI. |
| D2 | 2. Update `docs/project/browser-control-qa-matrix.md` Project/import row for the new controls and tests. | complete | `docs/project/browser-control-qa-matrix.md` now names remote PractiScore connect, remote match select, selected-match import, clear session, and manual fallback file import, plus the linked Task C test owners. | The QA matrix now reflects the expanded Project/import control surface and its test ownership. |
| D3 | 3. Create `.github/copilot-instructions.md` with mandatory parity rules. | complete | New file: `.github/copilot-instructions.md`. It requires test, doc, and fallback-path updates whenever Project-pane PractiScore workflow or state changes. | Repository-level guidance now codifies the parity rules required by Task C. |
| D4 | 4. Create `.github/AGENTS.md` describing the task boundaries and ownership model for PractiScore sync changes. | complete | New file: `.github/AGENTS.md`. It describes Task A/B/C ownership boundaries and escalation rules for cross-boundary changes. | The ownership model is now documented for future agent work in this feature area. |
| D5 | 5. Create `.github/instructions/practiscore-sync.instructions.md` with file-pattern scoped guidance for the source, tests, and docs touched by this feature area. | complete | New file: `.github/instructions/practiscore-sync.instructions.md` with `applyTo` patterns covering Task C source, tests, and docs, plus feature-specific guardrails. | The scoped agent instruction file now gives future edits a precise Task C feature boundary and parity checklist. |

## Human Verification Checklist

| Row | Checklist item | Status | Evidence | Validation note |
| --- | --- | --- | --- | --- |
| H1 | 1. Project pane shows Connect, remote-match selection, import, and clear-session controls. | complete | `src/splitshot/browser/static/index.html` adds those controls, and `tests/browser/test_browser_static_ui.py` asserts each Project-pane control ID and label. | The required controls are now present in the Project pane and covered by the static UI contract. |
| H2 | 2. Session and sync status are visible, stable, and understandable. | complete | `src/splitshot/browser/static/app.js::renderPractiScoreRemoteState()` renders explicit session/import status rows and state pills; `tests/browser/test_browser_interactions.py` covers the ready and expired states; `docs/userfacing/panes/project.md` explains the status area. | Users now get explicit, tested status output for both session and sync state changes. |
| H3 | 3. Manual file import still works. | complete | `tests/browser/test_browser_interactions.py::test_project_pane_manual_practiscore_file_import_remains_functional_with_remote_controls` passed; `uv run pytest tests/browser/test_browser_control.py -k practiscore` passed with `7 passed, 63 deselected`; `uv run pytest tests/browser/test_project_lifecycle_contracts.py -k practiscore` passed with `2 passed, 5 deselected`. | The existing manual PractiScore file-import path remains functional after the remote-sync UI changes. |
| H4 | 4. Browser coverage docs include the new controls and linked tests. | complete | `docs/project/browser-control-qa-matrix.md` now documents the new Project/import controls and test ownership, and `tests/browser/test_browser_control_coverage_matrix.py` passed. | The coverage documentation and its enforcement test stay aligned with the new Project-pane behavior. |
| H5 | 5. `.github` instruction files exist and codify parity expectations. | complete | `.github/copilot-instructions.md`, `.github/AGENTS.md`, and `.github/instructions/practiscore-sync.instructions.md` now exist and describe parity, ownership, and scoped guidance. | The repository now has persistent governance files that encode the Task C maintenance rules. |

## Final Summary

| Metric | Value |
| --- | --- |
| Total line items | 36 |
| Completed line items | 36 |
| Incomplete line items | 0 |
| Completion percent | 100% |

## Remaining Actions

None.