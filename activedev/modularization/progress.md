# Modularization Progress Ledger

This file is the operational ledger for task claims, completions, blockers, and proof links.

## Status values

- `pending` — task not yet claimed
- `claimed` — actively assigned to a run and locked for editing
- `blocked` — cannot proceed until dependency or ambiguity is resolved
- `done` — implementation, validation, audit, and proof are complete
- `waived` — intentionally skipped with explicit rationale

## Lock protocol

1. Read `plan.md`, the assigned task file, `validation.md`, `audit.md`, and this ledger.
2. Change the task row to `claimed` before editing any owned file.
3. Do not edit files outside the task's `touches-files` list.
4. When finished, add a proof file in `proof/`, then update the task row with proof and notes.
5. If blocked, update the row and add a short blocker note in the log section.

## Task status board

| Task | Title | Status | Depends on | Owner | Proof | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `T00` | Foundation and governance | `done` | none | `copilot-20260502-doc-run1` | `proof/PROOF-T00-run1.md` | Control-plane docs, task packets, and source-doc links created |
| `T01` | Baseline truth audit | `done` | `T00` | `copilot-orchestrator-20260502-t01-run1` | `proof/PROOF-T01-run1.md` | Verified live repo facts, updated audited source docs, and populated ownership anchors with no new blocker beyond `T02` |
| `T02` | QA baseline doc restoration | `done` | `T01` | `copilot-orchestrator-20260502-t02-run1` | `proof/PROOF-T02-run1.md` | Restored the QA matrix/coverage/full-e2e docs, reconciled developer docs, and passed the Tier A doc-audit tests after rebuilding a broken local `.venv` |
| `T03` | Bootstrap module shell | `done` | `T01`, `T02` | `copilot-orchestrator-20260502-t03-run1` | `proof/PROOF-T03-run1.md` | Validated the existing module-shell candidate; exact required Tier B suite passed after local virtualenv `ffmpeg`/`ffprobe` repair, and legacy globals remain intentionally for module-mode compatibility |
| `T04` | Backbone core | `done` | `T03` | `copilot-orchestrator-20260502-t04-run1` | `proof/PROOF-T04-run1.md` | Extracted `utils`, `event-bus`, and `store`; delegated owned `app.js` helper/state seams and passed the exact required browser validation scope |
| `T05` | Backbone runtime | `done` | `T04` | `copilot-orchestrator-20260502-t05-run1` | `proof/PROOF-T05-run1.md` | Extracted runtime backbone modules, preserved legacy compatibility shims, and passed the exact required browser validation scope |
| `T06` | Components shell | `done` | `T05` | `copilot-orchestrator-20260503-t06-run1` | `proof/PROOF-T06-run1.md` | Extracted the status-bar and video-player shell components, updated the owned browser contracts, and passed the exact required browser validation scope |
| `T07` | Components waveform and overlay | `done` | `T06` | `copilot-orchestrator-20260503-t07-run1` | `proof/PROOF-T07-run1.md` | Extracted waveform state/component and overlay canvas scheduler, updated owned browser contracts, and passed the exact required browser validation scope (`65 passed`) |
| `T08` | Pilot scoring pane | `done` | `T07` | `copilot-orchestrator-20260503-t08-run1` | `proof/PROOF-T08-run1.md` | Created a generic `pane-base.js`, extracted `scoring-pane.js`, updated owned scoring/browser contracts, and passed the exact required validation scope (`45 passed`) |
| `T09A` | Settings and metrics panes | `done` | `T08` | `copilot-orchestrator-20260503-t09a-run1` | `proof/PROOF-T09A-run1.md` | Extracted `settings-pane.js` and `metrics-pane.js`, preserved source-visible settings/metrics wrapper seams plus settings-defaults and metrics export behavior, and passed the exact required validation scope (`14 passed`) |
| `T09B` | Project and merge panes | `done` | `T08` | `copilot-orchestrator-20260503-t09b-run1` | `proof/PROOF-T09B-run1.md` | Extracted `project-pane.js` and `merge-pane.js`, preserved PractiScore/manual-import parity plus merge preview/source commit ordering, and passed the exact required validation scope (`29 passed`) |
| `T09C` | Export and review panes | `done` | `T08` | `copilot-orchestrator-20260503-t09c-run1` | `proof/PROOF-T09C-run1.md` | Extracted `export-pane.js` and `review-pane.js`, preserved app-level export/overlay compatibility seams, and passed the exact required validation scope (`61 passed`) |
| `T09D` | ShotML and overlay panes | `done` | `T09C` | `copilot-orchestrator-20260503-t09d-run1` | `proof/PROOF-T09D-run1.md` | Extracted `shotml-pane.js` and `overlay-pane.js`, updated owned overlay/browser contracts, preserved review text-box and popup/marker seams, stabilized same-project popup/export draft persistence plus popup motion fallback behavior, and passed the exact required validation scope (`65 passed`) |
| `T09E` | Markers and timing panes | `done` | `T09D` | `copilot-orchestrator-20260503-t09e-run1` | `proof/PROOF-T09E-run1.md` | Extracted `markers-pane.js` and `timing-pane.js`, preserved wrapper seams in `app.js`, stabilized the exact markers/timing validation lane, and passed the required scope (`75 passed`) |
| `T10` | Monolith cleanup | `done` | `T09A`, `T09B`, `T09C`, `T09D`, `T09E` | `copilot-orchestrator-20260504-t10-run3` | `proof/PROOF-T10-run3.md` | Run 3 completed the remaining owned cleanup: moved the legacy global installer into `lib/global-compat.js`, removed dead settings fallback duplication, permanently waived `data-table.js`, passed targeted validation only, and handed `LEGACY_WIRE_EVENTS_SOURCE_ANCHORS` plus the shared global/debounce/keepalive bridge seams to `T10.5` |
| `T10.5` | Cleanup bridge | `done` | `T10` | `copilot-orchestrator-20260504-t10.5-run1` | `proof/PROOF-T10.5-run1.md` | Run 1 retired `LEGACY_WIRE_EVENTS_SOURCE_ANCHORS`, narrowed the `installLegacyGlobalCompat(...)` page-global surface to the globals still used at runtime, kept `data-table.js` permanently waived, passed targeted validation only (`4 passed`), and unblocked `T11` |
| `T11` | CSS split | `pending` | `T10.5` | — | — | Unblocked by `T10.5`; preserve exact styling while splitting CSS, with broad certification still deferred to `T12` |
| `T12` | Final certification and PWA readiness | `pending` | `T10.5`, `T11` | — | — | Final broad browser/audit certification after `T10`, `T10.5`, and `T11` implementation work is complete |

## Log

| Date | Entry |
| --- | --- |
| `2026-05-02` | Initialized the modularization control workspace and reserved `T00` for the documentation/control-plane setup run. |
| `2026-05-02` | Completed `T00` with `proof/PROOF-T00-run1.md`; next required gate is `T01` baseline truth audit. |
| `2026-05-02` | Completed `T01` with `proof/PROOF-T01-run1.md`; verified branch, line counts, browser-test inventory, missing QA-doc status, and hotspot ownership anchors from the live repo. |
| `2026-05-02` | Completed `T02` with `proof/PROOF-T02-run1.md`; restored the missing browser QA docs, reconciled the linked developer docs, and passed the required Tier A doc-audit tests. |
| `2026-05-02` | Claimed `T03` for the bootstrap module shell; candidate workspace edits exist, but no proof file or Tier B validation result has been recorded yet. |
| `2026-05-02` | Retained `T03` under `copilot-orchestrator-20260502-t03-run1` for a rescue/finish run before any further owned-file edits; current goal is to validate the existing bootstrap candidate, reconcile any minimal test deltas, and write proof. |
| `2026-05-02` | Completed `T03` with `proof/PROOF-T03-run1.md`; validated the module-shell bootstrap candidate, documented the intentional legacy-global compatibility shim, and passed the exact required browser suite after restoring local `ffmpeg`/`ffprobe` availability inside the worktree virtualenv. |
| `2026-05-02` | Claimed `T04` under `copilot-orchestrator-20260502-t04-run1`; goal is to extract zero-dependency `utils`, `event-bus`, and `store` modules and delegate from `app.js` without changing browser-visible behavior. |
| `2026-05-02` | Completed `T04` with `proof/PROOF-T04-run1.md`; extracted the backbone-core `lib/` modules, updated the owned static shell contract for the new delegation seam, and passed the exact required browser validation scope. |
| `2026-05-02` | Claimed `T05` under `copilot-orchestrator-20260502-t05-run1`; goal is to extract runtime backbone modules for API coordination, layout, keyboard handling, processing indicators, and activity flows while preserving the existing compatibility shim and browser-visible behavior. |
| `2026-05-03` | Completed `T05` with `proof/PROOF-T05-run1.md`; extracted `activity`, `api`, `keys`, `layout`, and `processing` runtime modules, recorded the intentional compatibility shims, fixed same-project remote overwrite regressions inside T05 ownership, and passed the exact required browser validation scope (`133 passed`). |
| `2026-05-03` | Claimed `T06` under `copilot-orchestrator-20260503-t06-run1`; goal is to extract the shared status-bar and video-player shell components while preserving the exact DOM/browser behavior and keeping any table-helper extraction out unless real multi-area reuse justifies it. |
| `2026-05-03` | Completed `T06` with `proof/PROOF-T06-run1.md`; extracted `components/status-bar.js` and `components/video-player.js`, preserved the wrapper-based compatibility seam in `app.js`, updated the owned static/lifecycle browser contracts, and passed the exact required browser validation scope (`103 passed`). |
| `2026-05-03` | Claimed `T07` under `copilot-orchestrator-20260503-t07-run1`; goal is to extract waveform rendering/interaction and overlay-canvas seams into shared components while preserving the exact drag, zoom, pan, redraw, and review-adjacent behavior with no pane extraction leakage. |
| `2026-05-03` | Completed `T07` with `proof/PROOF-T07-run1.md`; extracted `components/waveform.js`, `components/overlay-canvas.js`, and `lib/waveform-state.js`, preserved the wrapper-based compatibility seam in `app.js`, updated the owned browser/static contracts, and passed the exact required browser validation scope (`65 passed`). |
| `2026-05-03` | Claimed `T08` under `copilot-orchestrator-20260503-t08-run1`; goal is to prove the pane-extraction pattern with a generic `pane-base.js`, extract `scoring-pane.js`, and preserve exact scoring/workbench/selected-shot behavior with no other pane leakage. |
| `2026-05-03` | Completed `T08` with `proof/PROOF-T08-run1.md`; extracted `panes/pane-base.js` and `panes/scoring-pane.js`, reduced the owned scoring anchors in `app.js` to delegation wrappers, updated the owned scoring/browser contracts, stabilized an owned markers browser assertion encountered during the exact reruns, and passed the exact required validation scope (`45 passed`). |
| `2026-05-03` | Claimed `T09C` under `copilot-orchestrator-20260503-t09c-run1`; goal is to extract `export-pane.js` and `review-pane.js`, keep the export trigger/order and review text-box workflows identical, and leave the overlay-render/drag core ready for `T09D`. |
| `2026-05-03` | Completed `T09C` with `proof/PROOF-T09C-run1.md`; extracted `panes/export-pane.js` and `panes/review-pane.js`, reduced the owned export/review anchors in `app.js` to thin delegates, updated the owned review source-contract assertions, retained the export trigger/order and overlay-render seams in `app.js` on purpose, and passed the exact required validation scope (`61 passed`). |
| `2026-05-03` | Claimed `T09D` under `copilot-orchestrator-20260503-t09d-run1`; goal is to extract `shotml-pane.js` and `overlay-pane.js`, preserve the existing ShotML/overlay source-visible seams in `app.js`, keep review text-box ownership with `review-pane.js`, and leave popup/marker ownership for `T09E`. |
| `2026-05-03` | Completed `T09D` with `proof/PROOF-T09D-run1.md`; extracted `panes/shotml-pane.js` and `panes/overlay-pane.js`, updated the owned overlay/browser contracts, preserved the review text-box and popup/marker seams, stabilized same-project popup/export draft persistence plus popup motion fallback behavior, and passed the exact required validation scope (`65 passed`). |
| `2026-05-03` | Claimed `T09E` under `copilot-orchestrator-20260503-t09e-run1`; goal is to extract `markers-pane.js` and `timing-pane.js`, keep source-visible wrapper signatures in `app.js` for the cross-lane browser/static contracts, and preserve marker authoring, timing workbench, waveform-selection, and seek behavior exactly. |
| `2026-05-03` | Completed `T09E` with `proof/PROOF-T09E-run1.md`; extracted `panes/markers-pane.js` and `panes/timing-pane.js`, preserved the source-visible markers/timing wrapper seams in `app.js`, stabilized the exact markers/timing browser lane, and passed the required validation scope (`75 passed`). |
| `2026-05-03` | Claimed `T09B` under `copilot-orchestrator-20260503-t09b-run1`; goal is to extract `project-pane.js` and `merge-pane.js`, preserve the Project-pane PractiScore/manual-import/browser-open contract plus same-project draft behavior, and keep merge PiP preview/source commit ordering identical behind thin `app.js` wrappers. |
| `2026-05-03` | Completed `T09B` with `proof/PROOF-T09B-run1.md`; extracted `panes/project-pane.js` and `panes/merge-pane.js`, preserved the Project-pane PractiScore/manual-import/browser-open contract plus same-project draft handling and merge preview/source commit ordering, updated the owned browser contracts, and passed the exact required validation scope (`29 passed`). |
| `2026-05-03` | Claimed `T09A` under `copilot-orchestrator-20260503-t09a-run1`; goal is to extract `settings-pane.js` and `metrics-pane.js`, preserve the source-visible wrapper seams and legacy globals in `app.js`, and keep settings defaults plus metrics rendering/export behavior identical behind thin delegates. |
| `2026-05-03` | Completed `T09A` with `proof/PROOF-T09A-run1.md`; extracted `panes/settings-pane.js` and `panes/metrics-pane.js`, preserved the source-visible settings/metrics wrapper seams plus settings-defaults and metrics export behavior, passed the required validation scope (`14 passed`), and fully unblocked `T10`. |
| `2026-05-03` | Claimed `T10` under `copilot-orchestrator-20260503-t10-run1`; goal is to remove retired monolith rendering/event wiring scaffolding, delete dead compatibility wrappers/globals that no longer serve the extracted modules, and leave `app.js` as bootstrap-only while passing the required Tier D browser + suite validation. |
| `2026-05-03` | Blocked `T10` with `proof/PROOF-T10-run1.md`; repaired the broken `app.js` bootstrap and restored targeted browser validation, but the remaining cleanup is contract-frozen by non-owned shared suites that still source-assert `wireEvents()` / wrapper seams and rely on the legacy page-global bridge. |
| `2026-05-03` | Reclaimed `T10` for a rescue run after a focused unblock review determined the task can still finish within current ownership if `app.js` keeps the minimum source/runtime compatibility shim surface while removing genuinely retired monolith logic behind it. |
| `2026-05-03` | Closed the `T10` rescue claim and reset the task to `blocked`; the workspace is stable again, but the remaining cleanup still requires shared contract migration or expanded ownership because browser suites outside the T10 touch list continue to pin `app.js` wrapper/global seams. |
| `2026-05-04` | Claimed `T10` under `copilot-orchestrator-20260504-t10-run2`; goal is to finish T10 as a contract-preserving shrink by moving remaining monolith event/render orchestration behind thin owned delegates, keeping only the minimum source-visible/runtime compatibility anchors needed for shared browser suites, and then passing the required Tier D validation/audit scope. |
| `2026-05-04` | Closed `T10` run 2 as `blocked` with `proof/PROOF-T10-run2.md`; current code extracts shell render/style/event orchestration into `lib/shell-runtime.js` and preserves bootstrap layout state, but the task is not done because the required Tier D validation/audit scope was intentionally not rerun per user request and `app.js` still retains compatibility anchors rather than a proven bootstrap-only surface. |
| `2026-05-04` | Control-plane update: reopened `T10` as `pending` for owned cleanup implementation with targeted validation only, inserted `T10.5` as the immediate post-`T10` cleanup-bridge / leftover-consolidation step, and deferred broad browser/audit certification to `T12` as the final gate. |
| `2026-05-04` | Completed `T10` under `copilot-orchestrator-20260504-t10-run3` with `proof/PROOF-T10-run3.md`; moved the legacy global installer into `lib/global-compat.js`, removed dead settings fallback duplication from `app.js`, permanently waived `data-table.js`, passed the exact targeted Tier D validation slice (`4 passed`), and handed the remaining bridge anchors (`LEGACY_WIRE_EVENTS_SOURCE_ANCHORS`, shared page-globals, and auto-apply / keepalive / draft-flush seams) to `T10.5`. |
| `2026-05-04` | Completed `T10.5` under `copilot-orchestrator-20260504-t10.5-run1` with `proof/PROOF-T10.5-run1.md`; retired `LEGACY_WIRE_EVENTS_SOURCE_ANCHORS`, migrated the lifecycle/export browser contracts onto `shell-runtime.js` and pane modules, narrowed the legacy global bridge to the remaining runtime-needed globals, kept `data-table.js` permanently waived, passed the targeted Tier D bridge slice (`4 passed`), and unblocked `T11`. |
