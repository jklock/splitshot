name: Build Electron

on:
  push:
    tags:
      - 'v*'

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-14, windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install uv
        run: pip install uv

      - name: Install system dependencies (macOS)
        if: runner.os == 'macOS'
        run: brew install ffmpeg

      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq \
            ffmpeg \
            libxcb-cursor0 libxkbcommon-x11-0 \
            libegl1 libegl-mesa0 libgl1 \
            libxrandr2 libglib2.0-0 libfontconfig1 \
            libdbus-1-3 libx11-xcb1 libpulse0 libopengl0 \
            libxcb-icccm4 libxcb-image0 libxcb-keysyms1 \
            libxcb-randr0 libxcb-render-util0 libxcb-shape0 \
            libxcb-xfixes0 libxcb-xinerama0 libxcb-xkb1 \
            libgtk-3-0 libnotify4 libnss3 libnspr4 \
            libxss1 libxtst6 libasound2t64 \
            libdrm2 libgbm1 libatspi2.0-0 libcups2 \
            build-essential python3-dev \
            libblas-dev liblapack-dev gfortran \
            libssl-dev pkg-config curl

      - name: Install system dependencies (Windows)
        if: runner.os == 'Windows'
        run: choco install ffmpeg -y --no-progress
        timeout-minutes: 5

      - name: Sync Python dependencies
        run: uv sync --extra dev
        timeout-minutes: 15

      - name: Verify CLI
        run: uv run splitshot --check

      - name: Bundle Python
        run: node scripts/bundle-python.js
        timeout-minutes: 20

      - name: Verify bundle
        run: node scripts/bundle-python.js check

      - name: Install Node dependencies
        run: cd electron && npm install
        timeout-minutes: 10

      - name: Run parity audit
        run: uv run python scripts/audits/electron_parity_audit.py --mode parity

      - name: Build Electron app (macOS)
        if: runner.os == 'macOS'
        timeout-minutes: 30
        env:
          CSC_LINK: ${{ secrets.MAC_CERT_BASE64 }}
          CSC_KEY_PASSWORD: ${{ secrets.MAC_CERT_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
        run: |
          set -euo pipefail
          cd electron

          # 1. Import cert to login keychain (persists after electron-builder runs)
          echo "$CSC_LINK" | base64 -d > /tmp/devid.p12
          security import /tmp/devid.p12 -k ~/Library/Keychains/login.keychain-db \
            -P "$CSC_KEY_PASSWORD" -T /usr/bin/codesign -A
          rm -f /tmp/devid.p12
          SHA="49C2A7CDD2C1C84BDE7F719CB4B359E61AF7227C"

          # 2. Package .app (electron-builder signs with our cert)
          npx electron-builder --mac --dir --publish=never

          # 3. Unlock login keychain and restore as default
          security unlock-keychain -p "" ~/Library/Keychains/login.keychain-db 2>/dev/null || true
          security default-keychain -s ~/Library/Keychains/login.keychain-db

          # 4. Verify cert is findable
          security find-identity -v -p basic | grep "$SHA" || echo "WARNING: cert not found in keychain"

          # 5. Sign ALL executables inside the .app (electron-builder misses these)
          APP="build/mac-arm64/SplitShot.app"
          find "$APP/Contents/Frameworks" -type f -perm -0100 -print0 | \
            xargs -0 -I{} codesign --force --options runtime --timestamp -s "$SHA" "{}" 2>/dev/null || true
          # Also sign frameworks themselves
          find "$APP/Contents/Frameworks" -name "*.framework" -type d -print0 | \
            xargs -0 -I{} codesign --force --options runtime --timestamp -s "$SHA" "{}" 2>/dev/null || true
          # Sign Python bundle executables
          find "$APP/Contents/Resources/bundle" -name "python*" -type f -print0 | \
            xargs -0 -I{} codesign --force --options runtime --timestamp -s "$SHA" "{}" 2>/dev/null || true

          # 5. Create DMG
          DMG="build/SplitShot-1.1.0-arm64.dmg"
          hdiutil create -volname "SplitShot" -fs HFS+ -srcfolder "$APP" "$DMG"

          # 6. Notarize
          xcrun notarytool submit "$DMG" --apple-id "$APPLE_ID" \
            --team-id "$APPLE_TEAM_ID" --password "$APPLE_APP_SPECIFIC_PASSWORD" --wait
          xcrun stapler staple "$DMG"

      - name: Build Electron app (Windows/Linux)
        if: runner.os != 'macOS'
        run: cd electron && npx electron-builder --publish=never
        timeout-minutes: 30

      - name: Build Electron app (Windows/Linux)
        if: runner.os != 'macOS'
        run: cd electron && npm run build
        timeout-minutes: 30

      - name: Verify installer
        shell: bash
        run: |
          shopt -s nullglob extglob
          found=( electron/build/*.@(dmg|exe|AppImage) )
          if [ ${#found[@]} -eq 0 ]; then
            echo "No installers found in electron/build/"
            exit 1
          fi
          for f in "${found[@]}"; do
            ls -lh "$f"
          done

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: splitshot-${{ runner.os }}
          path: |
            electron/build/*.dmg
            electron/build/*.exe
            electron/build/*.AppImage

  release:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: splitshot-*/*
          generate_release_notes: true
