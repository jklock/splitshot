> **Note:** Historical first-pass UI package. Do not execute directly. Current work starts in `docs/automate3` and `docs/automate3-ui`.


# Agent Rules

## Purpose

These rules apply to any implementation agent executing this UI package.

## Rules

- Treat `spec.md` as the UI truth source.
- Treat `docs/automate/` as the backend contract source.
- Do not invent new product surfaces beyond:
  - `Single Video`
  - `Multi Video`
  - `Performance Library`
- Do not copy competitor naming into UI, tests, or release notes.
- Keep `/api/state` summary-oriented.
- Use dedicated fetches for heavy UI datasets.
- Treat PiP smoothness as blocker number one.
- Treat `Match Recap` and `Stage Composite` as separate UI and proof tracks.
- Do not claim completion without targeted, visible-flow proof.
