> **Note:** This is the only current status ledger for Gen 2. Older automate docs are historical unless explicitly referenced here.

# Truth Audit Matrix

Audit last refreshed: 2026-05-22 after interaction fixes, screenshot script rewrite, and regenerated proof artifacts.

## Current Status

Automate3 is **not release-complete**, but the previous hard blockers are now remediated:

- route contract proof passes;
- browser interaction regressions are fixed;
- empty and loaded screenshots are generated by state-predicate scripts;
- loaded screenshots are nonblank and hash-distinct from empty screenshots;
- backup restore no longer silently swallows per-record failures.

Remaining gates:

- final human/vision visual approval;
- real export execution proof for `/api/workspace/export`;
- real recap-render proof for `/api/workspace/recap/render`;
- PiP/multi-angle and export-progress screenshots.

## Matrix

| Claim | Current Repo Truth | Gap | Status |
|---|---|---|---|
| Landing Page exists | Shell integration and recent activity route exist; empty screenshot captured with assertions | Returning-user screenshot still not captured | wired-needs-proof |
| Stage Video Edit exists | Loaded Stage screenshot shows `Clip1.MP4`, video, waveform, detected shots, and rail | Visual polish still needs approval | wired-needs-proof |
| Match Video Edit exists | Loaded Match screenshot shows 2 stage cards and recap panel | Export/recap behavior still proof-limited | wired-needs-proof |
| Performance Library exists | Loaded Library screenshot shows persisted records and backend totals | Selection/detail interaction still needs E2E proof | wired-needs-proof |
| Four views separate | `#view-landing`, `#view-stage`, `#view-match`, `#view-library` active-state assertions pass | None known | verified |
| Route wiring | `test_automation_ui_shell_contracts.py` passes; known unregistered route list is empty | Route existence is not behavioral completion | verified |
| Browser interactions | `test_browser_interactions.py` now passes 40/40 | Full browser suite still pending | verified-targeted |
| Screenshot proof | Empty and loaded scripts pass; loaded hashes differ from empty hashes | Missing PiP/export/returning screenshots | partial |
| DOM/layout assertions | Empty and loaded screenshot scripts assert active view, rail visibility, bounds, overflow, and loaded content predicates | Visual approval still required | partial |
| Export workflows | `/api/workspace/export` is registered and wired | No real file export proof yet | wired-needs-proof |
| Match recap | `/api/workspace/recap/render` is registered and wired through result-card resolution | No real recap render artifact yet | wired-needs-proof |
| Backup/restore | Backup persists; restore reports structured per-record errors | Restore happy-path persistence proof still targeted only | partial |
| Browser E2E proof | Route contract, interaction, full-app E2E, browser control, landing routes, and full browser suite pass | None known | verified |
| Canonical grouped runner | `run_test_suite.py --mode all-together --format table` passes | None known | verified |
| Visual review | Vision inspection confirmed generated loaded screenshots are content-bearing | Final product-quality approval still required | open |

## Latest Verified Commands

| Command | Result |
|---|---|
| `node --check src/splitshot/browser/static/app.js` | pass |
| `uv run python -m py_compile scripts/docs/capture_automate3_views.py scripts/docs/capture_loaded_views.py scripts/docs/setup_and_capture_loaded.py` | pass |
| `uv run pytest tests/browser/test_browser_control.py::test_library_backup_restore_reports_record_errors -q` | pass, 1/1 |
| `uv run pytest tests/browser/test_browser_interactions.py -q --maxfail=1` | pass, 40/40 |
| `uv run pytest tests/browser/ -q` | pass, 323/323 |
| `uv run python scripts/docs/capture_automate3_views.py` | pass |
| `uv run python scripts/docs/capture_loaded_views.py` | pass |
| `uv run python scripts/testing/run_test_suite.py --mode all-together --format table` | pass |

## Screenshot Evidence

- Empty proof: `docs/screenshots/automate3/proof-results.json`
- Loaded proof: `docs/screenshots/automate3/loaded-proof-results.json`
- Contact sheet: `docs/screenshots/automate3/contact-sheet-final.png`

Completion remains blocked until the remaining gates above are run and recorded.
