# Workflow Wizard (Archived)

The Workflow Wizard was removed from the live SplitShot browser shell and archived here for historical reference.

It previously presented the normal pane order from Project through Export as a guided pass over the live UI. The active shell now relies on the direct tool rail workflow instead.