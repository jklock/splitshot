# UX And Automation Refresh Todo

- Add controller methods for upload-and-analyze ingest
- Add UI status message plumbing for automatic processing feedback
- Replace the empty preview with a guided upload surface
- Support drag-and-drop primary upload
- Auto-analyze primary videos on upload
- Auto-analyze secondary videos on upload
- Add stat cards for shots, draw time, total time, and sync state
- Add top metrics for draw time, stage time, total shots, average split, and merge state
- Replace the default shell with workflow navigation inspired by Shot Streamer
- Separate review panels for preview, waveform, merge waveform, and splits
- Replace the split table-first review with split cards while preserving shot selection and editing
- Simplify the project tab so upload is the primary action and re-run analysis is secondary
- Disable or hide controls until their prerequisites exist
- Keep waveform editing, merge, scoring, overlay, and export features intact
- Add feature tests for automatic ingest and refreshed UX flow
- Write a post-refresh audit document
