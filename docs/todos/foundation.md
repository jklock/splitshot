# Foundation Todo

- Create `pyproject.toml`, `.python-version`, and `.gitignore`
- Define package layout under `src/splitshot`
- Add domain models, enums, and serialization helpers
- Add settings storage and defaults
- Add FFmpeg wrapper utilities
- Add local temporary/cache management
- Add application entrypoint

## Done Criteria

- `uv sync` succeeds
- `uv run splitshot` launches the desktop shell
- Base models serialize and deserialize cleanly
